% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patchDiagnostics.R
\name{getPatchDiagnostics}
\alias{getPatchDiagnostics}
\title{Diagnose patch assignments}
\usage{
getPatchDiagnostics(xy, X, patches, k = 10L, strict_k = NULL)
}
\arguments{
\item{xy}{Numeric matrix with cells in rows and x/y coordinates in columns.}

\item{X}{Numeric vector or matrix with cells in rows.}

\item{patches}{Final patch vector or the list returned by
\code{getPatches(log_iters = TRUE)}. \code{NA} denotes an unassigned cell. A vector
gives \code{NA} stability; a logged result compares the last two iterations and
assumes patch identifiers remain stable.}

\item{k}{Maximum number of spatial neighbors. Default 10, matching
\code{getPatches()}. Limited to \code{n - 1} for small datasets.}

\item{strict_k}{Neighbors used for \code{strict_component_fraction}. Must be no
greater than \code{k}. \code{NULL} uses \code{min(5, k)}.}
}
\value{
A list with three data frames:
\describe{
\item{patch_diagnostics}{One row per patch. A single \code{X} gives \code{x_sd}
and \code{x_n}; multiple columns give \verb{x_sd_<name>} and \verb{x_n_<name>}.
Other columns report patch size, strict connectivity, the first k with
full connectivity, and membership stability.}
\item{assignment_summary}{Cell assignment counts and effective k values.}
\item{connectivity_curve}{\code{patch}, \code{k}, and \code{component_fraction}, where
the fraction is the largest connected component divided by patch size.}
}
\code{patch_diagnostics} can be passed to \code{getPatchPolys()} or
\code{runInteractivePlotter()} as \code{patch_data}.
}
\description{
Report patch size, variation in \code{X}, spatial connectivity, assignment
coverage, and final membership stability when iteration logs are available.
}
\details{
Connectivity is calculated on symmetric k-nearest-neighbor graphs from 1 to
\code{k}. \code{strict_k} selects the graph used in \code{strict_component_fraction}.
\code{x_n} counts finite \code{X} values in each patch. \code{NA} values are excluded
from the SD; the SD is \code{NA} with fewer than two finite values or any
\code{NaN}/infinite value.
}
\examples{
## Use a subset of the example CRC data distributed with SpaceMosaic.
mini <- readRDS(system.file("extdata", "miniCRC.RDS", package = "SpaceMosaic"))
epithelial_cells <- which(mini$clust == "epi")
use <- epithelial_cells[seq_len(min(200L, length(epithelial_cells)))]
xy <- mini$xy[use, 1:2, drop = FALSE]
X <- mini$X[use]

set.seed(1)
patch_result <- getPatches(
  xy = xy,
  X = X,
  npatches = 5,
  n_iters = 3,
  log_iters = TRUE,
  verbose = FALSE
)

res <- getPatchDiagnostics(
  xy = xy,
  X = X,
  patches = patch_result
)
res$patch_diagnostics
res$assignment_summary

}
