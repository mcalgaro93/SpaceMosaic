% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neighbors.R, R/speWrappers.R
\name{embedCellNeighborhoods}
\alias{embedCellNeighborhoods}
\alias{embedCellNeighborhoods.spe}
\title{Embed cellular neighborhoods from single cell embeddings and positions}
\usage{
embedCellNeighborhoods(mat, xy, ks = c(5, 50), tissue = NULL)

embedCellNeighborhoods.spe(
  spe,
  embedding,
  ks = c(5, 50),
  tissue = NULL,
  name = "Z"
)
}
\arguments{
\item{mat}{Single cell embeddings matrix (cells x features).}

\item{xy}{2-column matrix of cells' positions.}

\item{ks}{Vector giving the number of nearest neighbors for each scale.
Default \code{c(5, 50)}.}

\item{tissue}{Optional vector giving tissue IDs to prevent cross-tissue
neighbor edges. Default NULL.}

\item{spe}{A SpatialExperiment object.}

\item{embedding}{Either a matrix of single cell embeddings (cells x
features, with one row per column of \code{spe}), or a single character
string giving the name of a reduced dimension already stored in
\code{reducedDim(spe)}.}

\item{name}{Character string giving the name under which the result is
stored via \code{reducedDim(spe, name)}. Default \code{"Z"}.}
}
\value{
A matrix of cellular neighborhood embeddings,
dimensions n cells x (ncol(mat) * length(ks)).

\code{spe} with a new reduced dimension, named according to
\code{name}, added via \code{reducedDim(spe, name)}. This matrix has
dimensions n cells x (ncol(embedding_mat) * length(ks)).
}
\description{
Creates a neighborhood embedding by averaging a cell embedding matrix over
spatial neighbor networks at multiple scales.
}
\section{Functions}{
\itemize{
\item \code{embedCellNeighborhoods.spe()}: Method for \code{SpatialExperiment}
objects. Resolves \code{embedding} from \code{reducedDims(spe)} (or takes it as a
matrix directly), runs \code{embedCellNeighborhoods()} against
\code{spatialCoords(spe)}, and stores the result back as a new reduced
dimension on \code{spe}. See \code{embedCellNeighborhoods} for how the
multi-scale neighborhood averaging works.

}}
