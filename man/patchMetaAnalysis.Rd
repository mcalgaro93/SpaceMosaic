% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metaanalysis.R
\name{patchMetaAnalysis}
\alias{patchMetaAnalysis}
\title{Use local meta-analysis to strengthen per-patch DE results}
\usage{
patchMetaAnalysis(
  DEobj,
  W,
  k = 15,
  min_effect = 0,
  max_pval = 0.05,
  min_patches = 3
)
}
\arguments{
\item{DEobj}{Results of patchDE. A list keyed by variable name, each with
$ests, $ses, $pvals matrices (genes x patches).}

\item{W}{Matrix of patch attributes (patches x features). Rownames must
match the column names of the DE matrices.}

\item{k}{Number of nearest neighbors to use. Default 15.}

\item{min_effect}{Minimum absolute posterior estimate for a patch to be
considered significant in subgroup detection. Default 0.}

\item{max_pval}{Maximum posterior p-value for significance. Default 0.05.}

\item{min_patches}{Minimum number of patches in a subgroup. Smaller
connected components are set to 0. Default 3.}
}
\value{
A list in the same structure as DEobj, with updated estimates,
SEs, and p-values derived from the local meta-analysis, plus a
\code{$subgroups} matrix (genes x patches) with positive cluster IDs
(1, 2, ...) for significant positive regions, negative IDs (-1, -2, ...)
for significant negative regions, and 0 elsewhere.
}
\description{
For each patch, finds k nearest neighbors in W-space, computes an
inverse-variance-weighted prior from those neighbors' estimates,
then performs a Bayesian normal-normal update with the patch's own estimate.
}
