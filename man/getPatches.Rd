% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ellipse.R
\name{getPatches}
\alias{getPatches}
\title{Assign cells to patches using elliptical Gaussian assignments}
\usage{
getPatches(
  xy,
  X,
  npatches,
  Z = NULL,
  alpha = 0.5,
  beta = 1,
  hunger_weight = 0.5,
  max_elongation = 4,
  max_radius = 3,
  n_iters = 15,
  log_iters = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{xy}{Matrix of cells' xy positions. Must have rownames.}

\item{X}{Numeric vector or matrix of design variables, aligned to rows of xy.
If a vector, treated as a single column. Each column is scaled to unit SD.}

\item{npatches}{Number of patches to create.}

\item{Z}{Optional matrix of per-cell context embeddings (cells x features).}

\item{alpha}{Weight of the Z penalty. Default 0.5.}

\item{beta}{Weight of the X diversity boost. Default 1.}

\item{hunger_weight}{Controls how aggressively low-variance patches grab cells.
0 = uniform hunger, 1 = hunger proportional to 1/totvar. Default 0.5.}

\item{max_elongation}{Maximum ratio of largest to smallest eigenvalue of a
patch covariance matrix. Constrains how narrow patches can be. Default 4.}

\item{max_radius}{Maximum Mahalanobis radius for assignment. Cells beyond
this threshold get zero spatial score for the patch. Default 3.}

\item{n_iters}{Number of outer iterations. Default 15.}

\item{log_iters}{If TRUE, return a list with patch assignments plus
per-iteration diagnostics (SS per patch and membership). Default TRUE.}

\item{verbose}{Show progress. Default TRUE.}
}
\value{
If log_iters = FALSE, a named vector of patch assignments.
If log_iters = TRUE, a list with:
\item{patch}{Named vector of final patch assignments.}
\item{ss_log}{Matrix (npatches x n_iters) of per-patch sum-of-squares of X.}
\item{membership_log}{Matrix (n_cells x n_iters) of patch assignments per iteration.}
}
\description{
Iterative EM-like algorithm:
(a) Estimate per-patch centroid + covariance from xy.
(b) Assign cells using spatial fit + X-diversity boost + Z-penalty + hunger.
(c) Re-estimate per-patch centroid + covariance.
(d) Assign cells using spatial fit only (no X/Z/hunger).
(e) Contiguity check: set orphans to NA.
}
