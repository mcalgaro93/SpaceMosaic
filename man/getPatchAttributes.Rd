% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metaanalysis.R, R/speWrappers.R
\name{getPatchAttributes}
\alias{getPatchAttributes}
\alias{getPatchAttributes.spe}
\title{Compute patch attributes matrix W from Z and patch assignments}
\usage{
getPatchAttributes(Z, patch)

getPatchAttributes.spe(spe, dimred = "Z", patch_col = "patch")
}
\arguments{
\item{Z}{Matrix or data.frame of per-cell features (cells x features).
If a data.frame is supplied, it is converted to a numeric design matrix
using \code{model.matrix(~ . - 1, data = Z)}.}

\item{patch}{Character vector of patch assignments (may contain NA).}

\item{spe}{A SpatialExperiment object.}

\item{dimred}{Character or integer scalar specifying which entry of
\code{reducedDims(spe)} to use as Z. Default \code{"Z"}.}

\item{patch_col}{Character scalar naming the column of \code{colData(spe)}
containing patch assignments. Default \code{"patch"}.}
}
\value{
Matrix (npatches x features) of per-patch mean attributes.
}
\description{
Takes colMeans of Z for each patch.
}
\section{Functions}{
\itemize{
\item \code{getPatchAttributes.spe()}: Method for \code{SpatialExperiment} objects.
Extracts a reducedDim matrix and a colData patch assignment column from
\code{spe}, then dispatches to \code{getPatchAttributes()}. \code{spe}'s \code{dimred} and
\code{patch_col} correspond to the \code{Z} and \code{patch} arguments of
\code{getPatchAttributes()} (renamed here since they now identify columns
rather than being passed as data directly); see \code{getPatchAttributes}
for how the per-patch means are computed.

}}
