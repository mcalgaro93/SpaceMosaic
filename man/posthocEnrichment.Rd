% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc.R
\name{posthocEnrichment}
\alias{posthocEnrichment}
\title{Post-hoc enrichment of DE-significant patch sets}
\usage{
posthocEnrichment(
  meta,
  attrib,
  z_cutoff = 2,
  meta_variables = NULL,
  gene_subset = NULL,
  adjust_for = NULL,
  min_pos = 5,
  p_adjust_method = "BH",
  verbose = TRUE
)
}
\arguments{
\item{meta}{Output of \code{patchMetaAnalysis()}: named list where each entry has
\code{ests} and \code{ses} matrices (genes x patches).}

\item{attrib}{Matrix/data.frame of patch attributes (patches x variables),
typically from \code{getPatchAttributes()}. Rownames must be patch IDs.}

\item{z_cutoff}{Z-stat threshold for significance. Defaults to 2.}

\item{meta_variables}{Optional character vector selecting which entries in
\code{names(meta)} to analyze. If NULL (default), analyze all.}

\item{gene_subset}{Optional character vector of genes to analyze. If NULL
(default), analyze all genes available for each DE variable.}

\item{adjust_for}{Optional character vector of \code{attrib} column names to include
as adjustment covariates in every model.}

\item{min_pos}{Minimum number of positive patches required to attempt a model
for a given gene-direction combination. Defaults to 5.}

\item{p_adjust_method}{Method passed to \code{stats::p.adjust()} for q-values.
Defaults to "BH".}

\item{verbose}{Show progress. Default TRUE.}
}
\value{
A list with:
\item{results}{Data frame with one row per
DE-variable x gene x direction x tested-attribute regression. Columns:
\code{de_variable}, \code{gene}, \code{direction}, \code{attribute}, \code{n_patches_used},
\code{n_sig}, \code{n_nonsig}, \code{log_odds}, \code{se}, \code{p_value}, \code{q_value}, \code{converged},
\code{fit_note}.}
\item{skipped}{Data frame with skipped model attempts and reason.}
\item{params}{Named list with function arguments used.}
}
\description{
For each DE variable in a \code{patchMetaAnalysis()} result, this function builds
direction-specific binary outcomes per gene (up: z > z_cutoff,
down: z < -z_cutoff), then runs logistic regression for each attribute
column to quantify enrichment in significant patches.
}
\details{
The tested attribute is fit one-at-a-time, optionally adjusting for
user-specified confounders from \code{attrib}.
}
