% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DE.R, R/speWrappers.R
\name{patchDE}
\alias{patchDE}
\alias{patchDE.spe}
\title{patchDE: run DE over all patches}
\usage{
patchDE(
  y,
  df,
  patch,
  pearson = FALSE,
  tot = NULL,
  resid_mse = FALSE,
  return_residuals = FALSE,
  verbose = TRUE
)

patchDE.spe(
  spe,
  df,
  assay_name = "logcounts",
  patch_column = "patch",
  pearson = FALSE,
  tot = NULL,
  resid_mse = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{y}{Expression matrix, cells * genes}

\item{df}{Either a character vector naming column(s) of \code{colData(spe)}
to use as DE predictors, or a matrix with one row per column of
\code{spe} (cells), used as-is.}

\item{patch}{Vector of patch IDs}

\item{pearson}{Logical; if TRUE, transform y to Pearson residuals before DE}

\item{tot}{Numeric vector of total counts per cell (required if pearson = TRUE)}

\item{resid_mse}{Logical; if TRUE, include per-gene residual MSE in output}

\item{return_residuals}{Logical; if TRUE, return an OLS residual matrix
aligned with the rows and columns of \code{y}. Default FALSE.}

\item{verbose}{Show progress. Default TRUE.}

\item{spe}{A SpatialExperiment object.}

\item{assay_name}{Character string giving the name of the assay in
\code{spe} to use as the expression matrix (cells x genes after
transposition). Default "logcounts".}

\item{patch_column}{Character string giving the column of
\code{colData(spe)} that holds patch IDs for each cell. Default "patch".}
}
\value{
A list keyed by model variable. Each element contains \code{pvals},
\code{ests}, and \code{ses} matrices with genes in rows and patches in columns.
If \code{resid_mse = TRUE}, the list also contains a \code{resid_mse} matrix with
the same orientation. If \code{return_residuals = TRUE}, a list with components
\code{de} (the usual result) and \code{residuals} is returned instead. \code{residuals} is
a cells by genes matrix aligned with \code{y}; rows whose patch is missing
contain \code{NA}.
}
\description{
patchDE: run DE over all patches
}
\section{Functions}{
\itemize{
\item \code{patchDE.spe()}: Method for \code{SpatialExperiment} objects. Extracts
the expression matrix from the chosen assay and resolves \code{df} from
\code{colData(spe)}, then dispatches to \code{patchDE()}. See \code{patchDE} for
details of the DE model and the meaning of \code{pearson}, \code{tot}, and
\code{resid_mse}.

}}
\examples{
y <- cbind(gene_a = c(2, 4, 5, 8, 9, 13))
df <- data.frame(treatment = c(0, 0, 1, 1, 2, 2))
patch <- rep("patch_1", nrow(y))

fit <- patchDE(
  y, df, patch,
  return_residuals = TRUE,
  verbose = FALSE
)
fit$de$treatment$ests
fit$residuals

}
