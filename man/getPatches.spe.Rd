% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speWrappers.R
\name{getPatches.spe}
\alias{getPatches.spe}
\title{Run the SpaceMosaic patching workflow on a \code{SpatialExperiment}}
\usage{
getPatches.spe(
  spe,
  X,
  npatches,
  Z = NULL,
  alpha = 0.5,
  beta = 1,
  hunger_weight = 0.5,
  max_elongation = 4,
  max_radius = NULL,
  mahal_radius = 3,
  n_candidates = 20,
  n_iters = 15,
  init_method = c("kmeans", "gradient_ellipse"),
  init_gradient_k = 30,
  init_gradient_elongation = 4,
  x_weighted_ellipse_second_pass = FALSE,
  x_ellipse_gamma = 1,
  x_ellipse_wmax = 3,
  log_iters = TRUE,
  patch_diagnostics = TRUE,
  k = 10L,
  strict_k = NULL,
  patch_polys = TRUE,
  patch_column = "patch",
  verbose = TRUE
)
}
\arguments{
\item{spe}{A \code{SpatialExperiment} object. Must have \code{colnames} (cell
identifiers), non-empty \code{spatialCoords()}, and the columns named in \code{X}
present in \code{colData(spe)}.}

\item{X}{Design variables for each spatial unit. A character vector of
column names in \code{colData(spe)}. Each column is scaled to unit SD by
\code{getPatches()}.}

\item{npatches}{Number of patches to create.}

\item{Z}{Optional per-cell context embeddings (cells x features). A single
character string naming an entry in \code{reducedDimNames(spe)}. If supplied,
patches will prefer Z-coherent regions. \code{NULL} (default) disables.}

\item{alpha}{Weight of the Z penalty. Typical range 0.2--1.0; default 0.5.
Higher values force patches to respect microenvironment boundaries at the
cost of spatial compactness. Set to 0 to ignore Z entirely.}

\item{beta}{Weight of the X diversity boost. Typical range 0.5/K--3/K where
K = \code{length(X)}; default 1 (appropriate for a single design variable). For
multiple design variables, scale down proportionally (e.g. \code{beta = 0.2}
for K = 5). Raise if patches are too homogeneous; lower if patches are
spatially fragmented.}

\item{hunger_weight}{Controls how aggressively low-variance patches grab
cells. Typical range 0.3--0.7; default 0.5. 0 = all patches equally hungry
(uniform sizes), 1 = hunger proportional to 1/totvar (maximizes variance
reduction but allows extreme size imbalance).}

\item{max_elongation}{Maximum ratio of largest to smallest eigenvalue of a
patch covariance matrix. Typical range 2--8; default 4. Lower values force
rounder patches.}

\item{max_radius}{Maximum Euclidean distance from a patch centroid for
assignment. Cells beyond this get zero spatial score. Default \code{NULL}
(auto: 3x the expected patch radius assuming uniform circular patches).}

\item{mahal_radius}{Maximum Mahalanobis radius for assignment. Cells beyond
this (in each patch's own coordinate system) get zero spatial score.
Typical range 2--4; default 3. \code{Inf} disables the cutoff entirely.}

\item{n_candidates}{Number of nearest patch centroids to evaluate per cell.
Typical range 10--50; default 20. Higher values are more accurate but
slower.}

\item{n_iters}{Number of outer iterations. Typical range 10--30; default 15.}

\item{init_method}{Initialization method for patch seeds. \code{"kmeans"}
(default) uses isotropic k-means seeding; \code{"gradient_ellipse"} orients
initial ellipses along the local spatial gradient of X and currently
requires \code{length(X) == 1}.}

\item{init_gradient_k}{Number of nearest neighbors used to estimate local
spatial gradients of X when \code{init_method = "gradient_ellipse"}. A finite
integer >= 3; default 30.}

\item{init_gradient_elongation}{Target initial ellipse elongation ratio
(major/minor eigenvalue ratio) for \code{init_method = "gradient_ellipse"}.
Finite and >= 1; default 4.}

\item{x_weighted_ellipse_second_pass}{Logical; if \code{TRUE}, the ellipse re-fit
up-weights cells whose X is farther from their patch mean, which can
encourage elongated patches along smooth X gradients. Applied only when
\code{length(X) == 1}. Default \code{FALSE}.}

\item{x_ellipse_gamma}{Strength of X-based up-weighting in second-pass
ellipse fitting. 0 disables weighting; typical range 0.5--1.5; default 1.}

\item{x_ellipse_wmax}{Cap on standardized X-deviation used in the weighting
rule, to limit outlier influence. Typical range 2--4; default 3.}

\item{log_iters}{Logical; if \code{TRUE} (default), per-iteration diagnostics are
retained and stored in
\code{metadata(spe)$SpaceMosaic$patch_iterations}.}

\item{patch_diagnostics}{Logical; if \code{TRUE} (default), compute patch-level
diagnostics with \code{getPatchDiagnostics()} and merge its elements
(\code{patch_diagnostics}, \code{assignment_summary}, \code{connectivity_curve}) into
\code{metadata(spe)$SpaceMosaic}.}

\item{k}{Maximum number of spatial neighbors used by \code{getPatchDiagnostics()}.
Default 10. Limited internally to \code{n - 1} for small datasets. Ignored when
\code{patch_diagnostics = FALSE}.}

\item{strict_k}{Neighbors used for \code{strict_component_fraction} in
\code{getPatchDiagnostics()}. Must be no greater than \code{k}; \code{NULL} (default)
uses \code{min(5, k)}. Ignored when \code{patch_diagnostics = FALSE}.}

\item{patch_polys}{Logical; if \code{TRUE} (default), compute patch polygons with
\code{getPatchPolys()} and store the result in
\code{metadata(spe)$SpaceMosaic$patch_polys}. When \code{patch_diagnostics = FALSE},
polygons are built without diagnostic information (\code{patch_data = NULL}).}

\item{patch_column}{Column name in \code{colData(spe)} where patch assignments are
stored. Default \code{"patch"}. An existing column of the same name is
overwritten.}

\item{verbose}{Logical; show progress. Default \code{TRUE}.}
}
\value{
The input \code{spe}, with results attached:
\itemize{
\item \code{colData(spe)[[patch_column]]}: named vector/factor of final patch
assignments, ordered to match \code{colnames(spe)}. \code{NA} denotes an
unassigned cell.
\item \code{metadata(spe)$SpaceMosaic$patch_iterations}: if
\code{log_iters = TRUE}, a list with \code{patch} (final assignments),
\code{membership_log} (n_cells x n_iters matrix of patch assignments per
iteration) and \code{ss_log} (npatches x n_iters matrix of per-patch
sum-of-squares of X across iterations).
\item \code{metadata(spe)$SpaceMosaic$patch_diagnostics},
\verb{$assignment_summary} and \verb{$connectivity_curve}: the three data frames
returned by \code{getPatchDiagnostics()}, if \code{patch_diagnostics = TRUE}.
\item \code{metadata(spe)$SpaceMosaic$patch_polys}: patch polygons, if
\code{patch_polys = TRUE}.
}
}
\description{
Convenience wrapper for the SpaceMosaic patching workflow on a
\code{SpatialExperiment}. This wrapper contains three steps:
\code{getPatches()} identifies spatial patches, \code{getPatchDiagnostics()} computes
patch-level information from the resulting patch assignments, and
\code{getPatchPolys()} constructs patch polygons using the patch assignments and
diagnostic information. Results are stored directly in the
\code{SpatialExperiment} object.
}
\details{
Spatial coordinates are taken from \code{spatialCoords(spe)}, design variables
from \code{colData(spe)}, and (optionally) context embeddings from
\code{reducedDims(spe)}. All results are returned attached to \code{spe}.
}
\examples{
library(SpatialExperiment)
spe <- readRDS(system.file("extdata", "cosmx_carcinoma.rds", package = "SpaceMosaic"))
spe <- getPatches.spe(
  spe = spe,
  X = "distance_to_fibroblast",
  npatches = 50
)
head(spe$patch)
head(metadata(spe)$SpaceMosaic$patch_diagnostics)

}
\seealso{
\code{\link[=getPatches]{getPatches()}}, \code{\link[=getPatchDiagnostics]{getPatchDiagnostics()}}, \code{\link[=getPatchPolys]{getPatchPolys()}}
}
